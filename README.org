The default machine is set to be dmAppointmentPlus.ts,
but you can comment it out and then comment in dmSmartHome.ts in the index.s file.



* Colour changer (React + xstate + Webspeech API)
To run:
#+begin_src sh
  npm install
  npm start
#+end_src
